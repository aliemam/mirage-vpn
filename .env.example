# Domain configuration
# Set up DNS records for these subdomains pointing to your server IP
DNSTT_DOMAIN=t.yourdomain.com
SLIPSTREAM_DOMAIN=s.yourdomain.com

# Tunnel password for SSH access through dnstt
TUNNEL_PASSWORD=your_secure_password_here

# Server IP (for reference)
SERVER_IP=your.server.ip
